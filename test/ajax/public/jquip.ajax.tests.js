test("should processed data", function(){
  expect(1);
  stop();

  var data = {
    name: 'trucks',
age: '27'},
      options = {
      url: '/echo',
      method: 'POST',
      data: dude,
      success: function(data) {
        data.headers.
        start();
  },
error: function() { ok(false, 'error'); start(); }
};

  jquip.ajax();
});
